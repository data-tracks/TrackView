import { defineStore } from 'pinia'

export const useStopStore = defineStore('stop', () => {
  const addSource = () => {

  }

  const addDestination = () => {

  }

  return { addSource, addDestination }
})
